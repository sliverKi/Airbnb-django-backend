from django.urls import path
from . import views

urlpatterns = [
    # path("", views.see_all_rooms),
    # path("<int:room_pk>", views.see_one_room),
    # path("<parameter_type:parameter_name>", view.def_name)
    # path("<str:room_name>/<int:room_id>", views.see_one_room),
]
